machine:
  environment:
    XCODE_PROJECT: IgnoreMe.xcodeproj
    XCODE_SCHEME: IgnoreMe
  xcode:
    version: "6.3.1"
dependencies:
  pre:
    - xcodebuild -version
    - xcodebuild -showsdks
    - pod --version
  override:
    - echo nothing to do
test:
  override:
    - make test && make clean
